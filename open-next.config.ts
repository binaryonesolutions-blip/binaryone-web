import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default Cloudflare Workers deployment config for OpenNext.
// Caching (R2 incremental cache, etc.) can be added here later if needed.
export default defineCloudflareConfig();
